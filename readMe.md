Might need for db querys with user
###docker exec -it db bash
###mysql -h localhost -P 3306 --protocol=tcp -uroot -pMYSQL_ROOT_PASSWORD
###grant ALL PRIVILEGES ON *.* TO 'MYSQL_USER';
###flush privileges